hackers-hangout/
│
├── public/ <-- Everything the browser can see (Frontend)
│ ├── index.html <-- Public Landing Page & Transmit Form
│ ├── dashboard.html <-- Secure Operator Terminal Layout
│ ├── css/
│ │ └── style.css <-- neon glows, reds, and blacks
│ └── js/
│ ├── main.js <-- Form submissions & UI interactions
│ └── dashboard.js <-- Handles dashboard tabs and feeds
│
├── server.js <-- Express Backend Engine
├── database.db <-- local SQLite database file (created later)
├── package.json <-- Node dependencies
└── ROADMAP.txt <-- MY DRAFT


KEEP IN MIND: 
I am new to web dev and programming and security. im using AI to learn. Im not copy pasting, but using AI as a teacher. I come up with ideas and ask for feedback so if you see thing here or anywhere in my repositories that are wrong just remmebr this is alla learning 
process for me. feel free to reach out with advice or tips and critiques. im all ears!

Rough draft blueprint idea:
index.html (has links to blog, signup, login) --> signup.html (handles signup and then redirects to login) --> login.html(logs user in and redirects to dasgboard) 
--> dashboard.html ( link to user profile, latest community activity from community feed, ltest opportunities like jobs or community projects that have been posted, link to messages). 
I will also creat an admin panel where i can address contack a hacker form submissions, send out system wide notifications, post to our blog, or post in community feed on company's behalf, 
take actions against user accounts and posts and other moderator actions, send mass messages to users.

Notes: 
ISSUE:
static html files cant protect themselves so we cant just have everything be html someone could just type website/dashboard and bypass login
FIX:
express needs to act as a gatekeeper using Middleware. when a user request /dashboard node should check thier security token (session or JWT). if they are authed then it /dashboard goes through if not they redirect to login page. 

ISSUE:
admin and normal users cant share general access routes. account compromise could lead to full database escalation.
FIX: 
Role-Based Access Control(RBAC). in SQLite every user needs a 'role' field (role:operator, role: admin)  the admin backend routes must strictly check for role === 'admin'. also shroud the admin path. instead of /admin do something
like /api/v1/system-override-control or something even more discrete.

ISSUE: 
automated system messages (Race Conditions). if admin triggers a message to 500 users at once inside a standard HTTP request loop the node server will lock up trying to proceessa ll the database writes at once and performance will drop to zero
for anyone else trying to use the app. basically Denial of Service
FIX:
for mass messaging and automated emails look into an asynchronous background event handler or a  lightweight queue. for my homelab scale i will just ensure my DB writes using non-blocking asynchronous operations (async/wait) so the main 
thread stays open to accept other incoming traffic